.DS_Store

dist/
e2e-smoke
/claver-agent

# Local-only credentials from the pre-relay direct-FCM setup. The agent no
# longer reads this path (see internal/push), but keep it ignored so a
# stray key never gets committed by an over-broad `git add`.
/server/

# Skills are materialized from skills-lock.json by the installer.
.agents/skills/
